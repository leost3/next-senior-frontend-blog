---
status: completed
title: 3 seed MDX posts
type: docs
complexity: low
dependencies:
  - task_07
---

# Task 14: 3 seed MDX posts

## Overview

Write three initial MDX posts in `content/posts/` so the blog is never empty at launch. Each post demonstrates a senior-level technical decision or tradeoff in the topics Leonardo writes about (performance, architecture patterns, codebase improvements, state management). Each post also exercises the custom MDX components (`Callout`, `CodeDiff`) built in task_07 to validate they render correctly in production.

<critical>
- ALWAYS READ the PRD and TechSpec before starting
- REFERENCE TECHSPEC for implementation details — do not duplicate here
- FOCUS ON "WHAT" — describe what needs to be accomplished, not how
- MINIMIZE CODE — show code only to illustrate current structure or problem areas
- TESTS REQUIRED — every task MUST include tests in deliverables
</critical>

<requirements>
- MUST create exactly 3 `.mdx` files in `content/posts/`
- Each file MUST have valid frontmatter: `title`, `date`, `description`, `tags` (array)
- Each post MUST be at least 400 words of substantive content (not placeholder lorem ipsum)
- Each post MUST include at least one fenced code block (to verify syntax highlighting)
- At least one post MUST use the `<Callout>` component
- At least one post MUST use the `<CodeDiff>` component
- Post topics MUST align with PRD-stated themes: performance, architecture patterns, codebase improvements, or state management
- Posts MUST demonstrate decision + tradeoff narrative (not a tutorial walkthrough)
</requirements>

## Subtasks

- [x] 14.1 Write post 1: performance topic (e.g., why a specific optimization was chosen over alternatives)
- [x] 14.2 Write post 2: architecture or state management topic (e.g., a data flow decision)
- [x] 14.3 Write post 3: codebase improvement topic (e.g., a refactor decision with measurable outcomes)
- [ ] 14.4 Verify all three posts render correctly at `/blog/[slug]` with no MDX errors
- [ ] 14.5 Verify `Callout` and `CodeDiff` components render correctly in the rendered posts

## Implementation Details

See PRD "User Stories — Author" for the authoring workflow.

See PRD "Core Feature 1 — Post Publishing via MDX in Git" for required frontmatter fields.

See TechSpec "Directory Structure" for post file location (`content/posts/*.mdx`).

Filename convention: use kebab-case matching the post topic (e.g., `understanding-react-render-batching.mdx`). The filename without extension becomes the slug.

### Relevant Files

- `content/posts/post-1.mdx` — create (rename to descriptive slug)
- `content/posts/post-2.mdx` — create
- `content/posts/post-3.mdx` — create
- `components/mdx/Callout.tsx` — exercised in at least one post (task_07)
- `components/mdx/CodeDiff.tsx` — exercised in at least one post (task_07)

### Dependent Files

- `public/search-index.json` — regenerated by `pnpm build` once posts exist (task_04)

## Deliverables

- 3 `.mdx` post files in `content/posts/`
- Unit tests with 80%+ coverage **(REQUIRED)**
- Integration tests for post rendering **(REQUIRED)**

## Tests

- Unit tests:
  - [x] Each `.mdx` file has valid frontmatter with non-empty `title`, `date`, `description`, and `tags`
  - [x] Each file is at least 400 words (validated by `calculateReadingTime` returning >= 2 minutes)
  - [x] `tags` field is an array with at least one element in each post
- Integration tests:
  - [ ] `GET /blog/[slug]` for each of the three posts returns 200 (not 404)
  - [ ] Each post page renders without React errors in the browser console
  - [ ] The post using `<Callout>` renders the callout element in the DOM
  - [ ] The post using `<CodeDiff>` renders the diff element in the DOM
  - [ ] `pnpm build` succeeds and generates routes for all three slugs in the static output
- Test coverage target: >=80%
- All tests must pass

## Success Criteria

- All tests passing
- Test coverage >=80%
- All three posts accessible at their `/blog/[slug]` URLs with correct content
- `Callout` and `CodeDiff` components render without errors
- `pnpm build` completes and the search index includes all three posts
